# the portfolio building
* the files for running the c `file` , `c++ file`, `java`and `python `
`the commit is added`

## Collaboration Log

- Pairing partner: marc raymond pedrin (GitHub: [@marcpedrin](https://github.com/marcpedrin))
- Built together: the `greet()` function.
- Learned: Live Share lets both people edit the same files in real time.
